/**
 * GTD Roadmap Module — ROADMAP.md parsing, phase extraction, progress tracking.
 * @module lib/roadmap
 *
 * Placeholder — full implementation in Phase 7 (forward pipeline).
 */

'use strict';

module.exports = function roadmap(args) {
  process.stdout.write(JSON.stringify({ status: 'not-yet-implemented', phase: 7 }));
};
